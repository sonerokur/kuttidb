# Pull-request validation for the C server and every client surface.
#
# The release workflows (release*.yml) run the same gates at tag time; this
# workflow makes them run on every push/PR touching code so a regression is
# caught before a tag exists, not after.
#
# Coverage follows docs/operations/RELEASE.md#release-gates-atomic-job-completion:
# core + job completion suites (state, completion, crash matrix with the
# test-only failpoints), protocol/SDK suites, and per-client job tests.
# Crash/recovery runs natively on the runner architecture (x86_64 Linux);
# other matrix cells stay gated by the release process, not this workflow.
name: PR validation

on:
  push:
    branches: ['*']
    paths-ignore:
      - 'docs/**'
      - '**.md'
  pull_request:
    paths-ignore:
      - 'docs/**'
      - '**.md'
  workflow_dispatch: {}

concurrency:
  group: pr-validation-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  core:
    name: Core, jobs, protocol
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build everything
        # `make` is intentionally the full build: the following gate executes
        # the standalone C test binaries as well as the server.
        run: make -j4 all core_test queue_test queue_crash_test atomic_test job_state_test job_completion_test

      - name: Core + job completion + crash matrix (make test, C suites)
        run: |
          set -e
          ./core_test
          ./job_state_test
          ./job_completion_test
          make job_crash_test_jobfailpoints
          ./job_crash_test_jobfailpoints
          ./queue_test && ./queue_crash_test && ./atomic_test

      - name: Protocol and client suites (Python)
        run: |
          set -e
          tmp=$(mktemp -d)
          trap 'rm -rf $tmp' EXIT
          KUTTIDB_JOB_TEST_PORT=0 python3 src/test_job_protocol.py
          KUTTIDB_JOB_SDK_PORT=0 python3 src/test_job_client.py

      - name: Release CMake build (tests under -UNDEBUG)
        run: |
          set -e
          cmake -S . -B build-pr -DCMAKE_BUILD_TYPE=Release
          cmake --build build-pr -j4
          ctest --test-dir build-pr --output-on-failure

      - name: Telemetry-enabled CMake build (release variant)
        # The release pipeline ships a telemetry-capable build with reporting
        # on by default; catch reporter (curl/OpenSSL) and default-mode
        # breakage here instead of at tag time.
        run: |
          set -e
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends libcurl4-openssl-dev
          cmake -S . -B build-pr-telemetry -DCMAKE_BUILD_TYPE=Release \
            -DKUTTIDB_TELEMETRY=ON -DKUTTIDB_TELEMETRY_DEFAULT_ON=ON
          cmake --build build-pr-telemetry -j4 --target kuttidb telemetry_config_test
          ./build-pr-telemetry/telemetry_config_test
          FEAT="$(./build-pr-telemetry/kuttidb --features)"
          printf '%s\n' "$FEAT" | grep -qx 'telemetry=v1'
          printf '%s\n' "$FEAT" | grep -qx 'telemetry-default=on'

  cli:
    name: kuttidb-cli standalone binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build the standalone CLI binary
        # The release workflow ships this binary in every tarball; keep the
        # recipe green on every push instead of first failing at tag time.
        run: |
          set -e
          python3 -m pip install --disable-pip-version-check pyinstaller==6.22.2
          make kuttidb-cli-bin
      - name: Smoke test the binary against a fresh server
        run: |
          set -e
          make -j4 kuttidb
          tmp="$(mktemp -d)"
          trap 'rm -rf "$tmp"' EXIT
          ./kuttidb 7417 "$tmp/k.wal" 2>/dev/null & pid=$!
          sleep 1
          ./dist-cli/kuttidb-cli --help >/dev/null
          ./dist-cli/kuttidb-cli -p 7417 put pr-smoke ok
          [ "$(./dist-cli/kuttidb-cli -p 7417 get pr-smoke)" = "ok" ]
          ./dist-cli/kuttidb-cli -p 7417 health
          kill "$pid"; wait "$pid" || true

  go:
    name: Go client (incl. job completion)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Build and test
        run: |
          set -e
          make -j4 kuttidb libkuttidb_embed.so
          cd clients/go
          go vet ./...
          go test -run 'TestJobCompletion|TestQueueNamedConsumer' -count=1 -v .
      - name: Go embedding opt-in and clean-consumer matrix
        # Default recursive Go checks must build without KuttiDB C deps
        # (embed.go is tag-gated), the tagged embed smoke must still work
        # with the provisioned library, and a clean external consumer must
        # import the module under either CGO setting.
        run: |
          set -e
          make go-embed-smoke
          make go-external-consumer
          cd clients/go
          CGO_ENABLED=0 go test ./... -count=1

  java:
    name: Java client (incl. job completion)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Build and test
        run: |
          set -e
          make -j4 kuttidb
          cd clients/java
          mkdir -p target/classes target/test-classes
          javac -encoding UTF-8 -d target/classes $(find src/main/java -name '*.java')
          javac -encoding UTF-8 -cp target/classes -d target/test-classes Smoke.java JobSmoke.java
          java -cp target/classes:target/test-classes JobSmoke

  rust:
    name: Rust client (incl. job completion)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Build and test
        run: |
          set -e
          make -j4 kuttidb
          cd clients/rust
          cargo test --test job_completion

  node:
    name: Node.js client (incl. job completion)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Build and test
        run: |
          set -e
          make -j4 kuttidb
          cd clients/nodejs
          node job_smoke.js
